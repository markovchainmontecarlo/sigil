Apply this refactor slice now. Read the current repository state before editing.

Preserve the plan invariants and public behavior. Stay within the declared paths. Make the complete coherent change and report what changed. Do not add compatibility files, duplicate sources, adapters, or legacy paths unless the intent explicitly requires compatibility. Deterministic gates run outside this agent turn.

OVERALL INTENT:
{{INTENT}}

INVARIANTS:
{{INVARIANTS}}

SLICE:
{{SLICE}}
