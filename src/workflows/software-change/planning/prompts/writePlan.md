Turn the investigation and decisions into one candidate plan at {{OUT_FILE}}.

Apply this planning rubric:
{{RUBRIC}}

Include goal, architecture, constraints, non-goals, and a requirement-to-task coverage table. Order tasks by dependency. Each task must include:

- a stable id, title, and summary;
- dependencies;
- produced interfaces and consumed interfaces that name their producer task and output;
- observable acceptance criteria;
- focused verification commands with expected results, or a manual procedure with its expected result and rationale;
- every affected repository-relative file, its action, and details anchored to stable symbols or structural locations;
- relevant test changes and diagrams only when they clarify the work.

Use line numbers only as supporting evidence. Do not make them the durable identity of an edit.

Finish with a self-review for complete requirement coverage, task size, placeholders, undefined names, and consistent interfaces and symbols. Write only the candidate plan.
