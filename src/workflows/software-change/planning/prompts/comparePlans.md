Sort these independent plans into where the agents agree and where they disagree. Work in this order:

1. Read every plan in full.

2. Write the convergence report as markdown to {{CONVERGE_FILE}}. Cover every point where the agents agree. For each point of agreement, write: the shared claim or task in one line; which planners assert it; the details each gave, including files, symbols, proposed change, and stated acceptance; and any wording or scope differences between the planners on that same point.

3. Write the divergence report as markdown to {{DIVERGE_FILE}}. Cover every point where the agents disagree, and every point only some agents raise. For each such point, write: the question in dispute in one line; each planner's position and which planner holds it; the details behind each position, including files, symbols, proposed change, and reasoning; and where a planner is silent on a point others raise, record that silence as its position.

GOAL / INTENT:
{{INTENT}}

PLANS:
{{PLANS}}
