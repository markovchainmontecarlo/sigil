Reopen the affected source and enrich this task graph in place at {{OUT_FILE}} for an implementer that receives one task at a time.

Add missing callers, test files, stable symbol anchors, dependency edges, produced and consumed interface links, runnable verification detail, and diagrams where they clarify the state flow. Preserve observable acceptance criteria. Treat file mechanisms as repository-grounded guidance rather than immutable design mandates.

Check each task independently for enough context to implement it without the earlier conversation. Add detail, never scope. Keep repository-relative paths and the contract shape.

INTENT:
{{INTENT}}

TASK GRAPH:
{{TASK_GRAPH}}
