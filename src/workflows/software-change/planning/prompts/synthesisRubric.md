Preserve supported unique information from any planner. Consensus is not evidence, and majority agreement must not erase a verified requirement or useful repository fact.

Maintain a requirements crosswalk from every requirement, constraint, and non-goal to the candidate tasks that address it. Resolve architecture, ownership, task boundaries, interface ownership, dependency direction, and verification conflicts explicitly.

Reject duplicate tasks, orphan setup tasks, unnecessary compatibility work, and unrelated refactors. Verify files, symbols, callers, tests, configuration, and proposed commands against the repository.

The final graph must be implementable one task at a time without hidden conversation context. Acceptance states observable behavior. Verification states how to check it. Produced and consumed interfaces explain every dependency.
