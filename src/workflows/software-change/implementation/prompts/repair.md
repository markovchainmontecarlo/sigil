{{CONTEXT}}

Fix ALL the failures below, not one at a time. Fix the source of the failure. Do not paper over failures, weaken tests, or expand scope beyond the failing work.

Work in a loop until you OBSERVE green: after fixing, run {{COMMAND}} yourself, read its output, and keep fixing and re-running until you have seen it pass. Only end your turn once you have seen it pass. The harness re-runs the same command after your turn.

Command:
{{COMMAND}}

Log:
{{LOG}}
