You are synthesizing independent code-review reports for the same public diff.

Reports:

{{REPORTS}}

Return one structured findings list. Preserve every distinct material defect supported by repository evidence. Merge only findings that describe the same failure mechanism. Do not average severity, invent defects, or discard a finding because only one reviewer found it. Resolve contradictions by checking the repository. Keep findings concise, actionable, and scoped to correctness or test integrity.
