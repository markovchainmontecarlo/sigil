interface:
  display_name: "Sigil Router"
  short_description: "Confirm a brief and route Sigil work"
  default_prompt: "Route the request with $sigil. Create and confirm one confirmed structured brief before state-changing work, preserve accepted state, and default to local implementation. Use Sigil-owned planning or external delivery only when the developer explicitly chooses it."
